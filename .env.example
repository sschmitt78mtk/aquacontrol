# SMTP Settings (credentials are managed via Web UI settings page)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_AUTH_EMAIL=
SMTP_AUTH_PASSWORD=
RECIPIENT_EMAIL=

# NTP
NTP_SERVER=fritz.box

# Application
SECRET_KEY=change-this-to-random-string
